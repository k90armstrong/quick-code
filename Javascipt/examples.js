// cloneNode(bool if deep "all children and attributes" true if is deep)
// example usage cloning a card with all of it's children
var card = document.getElementById('#card');
var clonedCard = card.cloneNode(true);